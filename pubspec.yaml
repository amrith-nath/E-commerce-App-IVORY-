name: ivory
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  animated_icon_button: ^1.0.2
  animations: ^2.0.7
  cached_network_image: ^3.2.2
  card_swiper: ^2.0.4
  cloud_firestore: ^4.0.3
  cupertino_icons: ^1.0.2
  equatable: ^2.0.5
  firebase_auth: ^4.1.0
  firebase_core: ^2.1.1
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  flutter_svg: ^1.1.5
  get: ^4.6.5
  google_fonts: ^3.0.1
  google_sign_in: ^5.4.2
  mirai_dropdown_menu: ^0.0.6+1
  modal_bottom_sheet: ^2.1.2
  razorpay_flutter: ^1.3.4
  salomon_bottom_bar: ^3.3.1
  shimmer: ^2.0.0
  super_rich_text: ^1.0.0

dev_dependencies:
  build_runner: ^2.3.2
  cloud_firestore_platform_interface: null
  cloud_functions_platform_interface: null
  firebase_auth_platform_interface: null
  firebase_database_platform_interface: null
  firebase_storage_platform_interface: null
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true

  assets:
    - asset/images/
    - asset/svgs/
    # - images/a_dot_ham.jpeg
