import 'package:flutter/material.dart';

class ScreenCatagories extends StatelessWidget {
  const ScreenCatagories({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('hello'),
      ),
    );
  }
}
